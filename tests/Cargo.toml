[package]
name = "tests"
version = "0.1.0"
edition = "2021"
readme = "README.md"
description = "A library crate for testing bowling ball scores."
authors = ["RedIsGaming <>, Dwarffish <>"]

[dependencies]
scores = { path = "../scores" }
